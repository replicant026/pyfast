<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Colorir Tags" Height="300" Width="300" 
        ResizeMode="NoResize" WindowStartupLocation="CenterScreen" Topmost="True">
    
    <StackPanel Margin="15">
        <TextBlock Text="Parâmetro para Colorir:" FontWeight="Bold" Margin="0,0,0,5"/>
        <TextBox x:Name="inputParametro" Height="25" Margin="0,0,0,5" Text="ID do comando"/>
        <TextBlock Text="(Ex: ID do comando, Marca, Tipo...)" Foreground="Gray" FontSize="10" Margin="0,0,0,20"/>

        <Button x:Name="btnColorir" Content="🎨 Colorir Tags" Height="40" Click="btn_colorir_click" Margin="0,0,0,10" FontWeight="Bold"/>

        <Button x:Name="btnLimpar" Content="🧹 Limpar Cores" Height="30" Click="btn_limpar_click" Margin="0,0,0,20"/>

        <TextBlock x:Name="txtStatus" 
                   Text="Aguardando..." 
                   HorizontalAlignment="Center" 
                   Foreground="Gray" 
                   TextWrapping="Wrap"/>
    </StackPanel>
</Window>